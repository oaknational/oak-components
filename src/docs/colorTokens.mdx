import { oakColorTokens } from "@/styles/theme";
import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";

<Meta title="style tokens/OakColorTokens" />

# OakColorTokens

<ColorPalette>
  {Object.entries(oakColorTokens).map((token, i) => {
    return <ColorItem key={i} title={token[0]} colors={[token[1]]} />;
  })}
</ColorPalette>
