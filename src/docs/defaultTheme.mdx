import { oakDefaultTheme } from "@/styles/theme";
import { parseColor } from "@/styles/helpers/parseColor";
import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";

<Meta title="style tokens/Theme tokens (oakDefaultTheme)" />

# oakDefaultTheme

### `OakUiRoleTokens`

<ColorPalette>
  {Object.entries(oakDefaultTheme.uiColors).map((token, i) => (
    <ColorItem
      key={i}
      title={`${token[0]} (${token[1]})`}
      colors={[parseColor(token[1])]}
    />
  ))}
</ColorPalette>
