import { oakDefaultTheme } from "@/styles/theme";
import { parseColor } from "@/styles/helpers/parseColor";
import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";

<Meta title="StyleTokens/oakDefaultTheme" />

# oakDefaultTheme

### uiColors
<ColorPalette>
  {Object.entries(oakDefaultTheme.uiColors).map((token, i) =>  <ColorItem key={i} title={token[0]} colors={[parseColor(token[1])]}/>)}
</ColorPalette>
