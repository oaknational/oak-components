import { Meta } from "@storybook/addon-docs/blocks";

import {
  oakAllSpacingTokens,
  oakSpaceBetweenTokens,
} from "@/styles/theme";
import { parseSpacing } from "@/styles/helpers/parseSpacing";
import { OakLI, OakUL } from "@/components/typography";

<Meta title="style tokens/Spacing tokens" />

# OakSpacingTokens

### oakAllSpacingTokens

<OakUL>
  {Object.entries(oakAllSpacingTokens).map((token, i) => (
    <OakLI key={i}>
      {token[0]}, {token[1]}px, {parseSpacing(token[0])}
    </OakLI>
  ))}
</OakUL>

### oakSpaceBetweenTokens

<OakUL>
  {Object.entries(oakSpaceBetweenTokens).map((token, i) => (
    <OakLI>
      {token[0]}, {token[1]}, {oakAllSpacingTokens[token[1]]}px,{" "}
      {parseSpacing(token[0])}
    </OakLI>
  ))}
</OakUL>
